import 'package:interview_prep/src/models/difficulty.dart';
import 'package:interview_prep/src/models/question.dart';

List<InterviewQuestion> expertQuestions() => const [
  // --- Internals Deep Dive ---
  InterviewQuestion(
    id: 'e_internal_1',
    topic: 'internals',
    subtopic: 'Widget-Element-RenderObject',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'How does Flutter\'s reconciliation algorithm work? How does it decide to update vs recreate elements?',
    answer: 'Flutter walks the widget tree top-down comparing new widgets with existing elements using Widget.canUpdate() — which checks runtimeType and key. If canUpdate returns true, the element is updated with the new widget config. If false, the old element is deactivated and a new one is created.',
    explanation: 'static bool canUpdate(Widget oldWidget, Widget newWidget) {\n  return oldWidget.runtimeType == newWidget.runtimeType && oldWidget.key == newWidget.key;\n}\n\nThis is why:\n- Changing widget type at a position causes full subtree rebuild\n- Keys allow preservation when widgets move in a list\n- const widgets always canUpdate (same instance)\n\nThe algorithm is O(n) — it\'s a linear walk, not a diff. Flutter avoids the O(n³) tree diff problem by making it the developer\'s responsibility to use keys correctly.',
  ),
  InterviewQuestion(
    id: 'e_internal_2',
    topic: 'internals',
    subtopic: 'Rendering Pipeline',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'Explain how repaint boundaries work internally. When should you add them?',
    answer: 'RepaintBoundary creates a separate compositing Layer for its subtree. When a child needs repainting, only its layer is repainted — parent and sibling layers are untouched. Add them around: frequently animating widgets, complex CustomPaint, scrollable item content.',
    explanation: 'Without RepaintBoundary: a small change repaints the entire layer (potentially the full screen).\nWith RepaintBoundary: the subtree gets its own OffsetLayer. Only that layer\'s content is re-rasterized.\n\nDevTools Paint indicator shows repaint regions. Green flashes = area being repainted. If you see the full screen flashing, add RepaintBoundary.\n\nOverusing RepaintBoundary wastes GPU memory (each layer is a texture). Profile before adding.',
  ),
  InterviewQuestion(
    id: 'e_internal_3',
    topic: 'internals',
    subtopic: 'Engine',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'How does Flutter communicate with the underlying platform (iOS/Android)?',
    answer: 'The Flutter engine (C++) hosts the Dart VM and provides: platform channels (binary messaging), a rendering surface (Metal/Vulkan/OpenGL), text layout (libtxt), networking (via platform), and plugin registration. The engine embeds in a platform-specific shell (FlutterViewController on iOS, FlutterActivity on Android).',
    explanation: 'Architecture layers:\n1. Framework (Dart) — widgets, rendering, gestures\n2. Engine (C++) — Dart VM, Skia/Impeller, text, platform channels\n3. Embedder (platform-specific) — window, input, lifecycle\n\nPlatform channels use binary serialization (StandardMethodCodec). Messages pass through the engine\'s binary messenger between Dart and the platform thread. This is asynchronous by design.',
  ),
  InterviewQuestion(
    id: 'e_internal_4',
    topic: 'internals',
    subtopic: 'Compilation',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'Explain Flutter\'s compilation modes: JIT, AOT, and kernel snapshots.',
    answer: 'Debug mode uses JIT (Just-In-Time) — compiles Dart to kernel bytecode, enables hot reload via incremental compilation. Release mode uses AOT (Ahead-Of-Time) — compiles to native ARM/x86 code for maximum performance. Profile mode is AOT with debugging symbols.',
    explanation: 'JIT (debug):\n- Dart VM interprets kernel bytecode\n- Hot reload: diff → kernel → update VM classes\n- Slower, larger binary, full debugging\n\nAOT (release):\n- dart2native compiles to machine code\n- No VM overhead, tree shaking removes dead code\n- Smaller binary, faster startup, no hot reload\n\nKernel snapshot: intermediate representation used by JIT. The analyzer transforms Dart → kernel → VM loads it.',
  ),

  // --- Performance Expert ---
  InterviewQuestion(
    id: 'e_perf_1',
    topic: 'performance',
    subtopic: 'Memory',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'How does Dart\'s garbage collector work? What are the implications for Flutter performance?',
    answer: 'Dart uses a generational, semi-space GC. Young generation: fast bump-pointer allocation, frequent minor GC (stop-the-world, <1ms). Old generation: mark-sweep-compact, infrequent, can cause jank. Implications: avoid promoting objects to old gen unnecessarily; const and immutable objects reduce GC pressure.',
    explanation: 'Young space: new objects allocated here. Minor GC copies live objects to the other semi-space. Dead objects are left behind (zero cost).\nOld space: objects that survive multiple minor GCs. Major GC is more expensive.\n\nFor Flutter:\n- Short-lived objects (widgets rebuilt each frame) stay in young gen → cheap GC\n- Leaking objects (forgotten listeners, unclosed streams) promote to old gen → expensive GC → potential jank\n- Use const constructors to allocate once, not per-frame',
  ),
  InterviewQuestion(
    id: 'e_perf_2',
    topic: 'performance',
    subtopic: 'Optimization',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'Your app has consistent jank (dropped frames) during scrolling. Walk through your debugging process.',
    answer: '1) Run in profile mode. 2) Open DevTools Performance tab, record scroll. 3) Identify if jank is in build, layout, paint, or rasterize phase. 4) If build: check Widget Inspector for excessive rebuilds, add const/extract widgets. 5) If paint: add RepaintBoundary, check for saveLayer/clipPath abuse. 6) If rasterize: reduce opacity layers, simplify shaders. 7) Check for work on main isolate that should be in compute().',
    explanation: 'Common scroll jank causes:\n- Building complex widgets on-demand without caching (ListView.builder helps)\n- Images decoding on main thread (use cacheWidth/cacheHeight)\n- Opacity widget creating saveLayer (use FadeTransition instead)\n- ClipRRect with complex clips (use clipBehavior: Clip.hardEdge)\n- Expensive computations in build (move to isolate)\n- Too many layers (merge small layers)',
  ),

  // --- Architecture Expert ---
  InterviewQuestion(
    id: 'e_arch_1',
    topic: 'architecture',
    subtopic: 'SOLID',
    difficulty: Difficulty.expert,
    type: QuestionType.scenario,
    question: 'You\'re building a feature that needs to fetch data from an API, cache it locally, sync changes, and handle offline mode. Design the architecture.',
    answer: 'Use a Repository with dual data sources (RemoteDataSource + LocalDataSource). The Repository decides which source to use based on connectivity. A SyncService handles background sync. Use sealed classes for data states (Fresh, Cached, Stale, Error). The UI layer observes Streams from the Repository for real-time updates.',
    explanation: 'Repository pattern with offline-first:\n1. Read: try local cache → if stale, fetch remote → update cache → emit\n2. Write: save locally → queue for sync → sync when online\n3. Conflict resolution: last-write-wins or merge strategy\n\nclass UserRepository {\n  Stream<DataState<User>> watchUser(String id) {\n    // emit cached data immediately\n    // fetch fresh data in background\n    // emit fresh data when available\n  }\n}',
  ),
  InterviewQuestion(
    id: 'e_arch_2',
    topic: 'architecture',
    subtopic: 'Clean Architecture',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'How do you handle cross-cutting concerns (logging, analytics, error reporting) without polluting business logic?',
    answer: 'Use middleware/interceptors: BLoC Observers for state management events, Dio interceptors for network calls, Navigator observers for screen tracking. For DI: create wrapper services that delegate to the real service + side-effect (logging). Aspect-Oriented approach via Dart annotations + code generation.',
    explanation: 'BlocObserver: override onChange, onEvent, onError for global state logging\nDio interceptors: log requests/responses, add auth headers, handle errors\nNavigatorObserver: track screen views for analytics\nErrorBoundary widget: catch and report rendering errors\n\nThe key principle: business logic should not know about analytics/logging. These concerns are injected via middleware at the infrastructure layer.',
  ),

  // --- Platform Expert ---
  InterviewQuestion(
    id: 'e_platform_1',
    topic: 'platform_integration',
    subtopic: 'FFI',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'How does Dart FFI work? When would you use it over platform channels?',
    answer: 'Dart FFI provides direct, synchronous calls to C libraries. It\'s faster than platform channels (no async message passing overhead). Use FFI for: high-frequency calls, performance-critical code (crypto, image processing), existing C/C++/Rust libraries. Use platform channels for: platform APIs (camera, GPS), UI-related operations, simple integrations.',
    explanation: 'FFI is synchronous and runs in the same thread — no serialization overhead.\nfinal dylib = DynamicLibrary.open("libcrypto.so");\nfinal hash = dylib.lookupFunction<NativeHash, DartHash>("sha256");\nfinal result = hash(data); // direct C call\n\nUse ffigen to auto-generate Dart bindings from C headers.\nFor Rust: use flutter_rust_bridge for safe FFI.',
  ),
  InterviewQuestion(
    id: 'e_platform_2',
    topic: 'platform_integration',
    subtopic: 'Pigeon',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'What is Pigeon and how does it improve platform channel communication?',
    answer: 'Pigeon is a code-generation tool that creates type-safe platform channel bindings. You define APIs in Dart, and Pigeon generates the Dart, Kotlin/Java, and Swift/ObjC code. This eliminates string-based method names, manual serialization, and type mismatches.',
    explanation: '@HostApi()\nabstract class BatteryApi {\n  int getBatteryLevel();\n}\n\nPigeon generates:\n- Dart: BatteryApi class with type-safe methods\n- Kotlin: BatteryApi interface to implement\n- The serialization/deserialization code\n\nBenefits: compile-time safety, no magic strings, auto-generated boilerplate, consistent API across platforms.',
  ),

  // --- Dart Expert ---
  InterviewQuestion(
    id: 'e_dart_1',
    topic: 'dart_fundamentals',
    subtopic: 'OOP',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'Explain Dart\'s type system: covariance, contravariance, and soundness.',
    answer: 'Dart\'s type system is sound — if the static type says String, it will always be a String at runtime. Generics are covariant by default (List<Cat> is a subtype of List<Animal>), which is technically unsound but pragmatic. Dart adds runtime checks to catch violations.',
    explanation: 'Covariance: List<Cat> can be assigned to List<Animal> ✓\nBut: List<Animal> animals = cats; animals.add(Dog()); // compiles but throws at runtime\n\nDart chose pragmatic covariance over Java-style wildcards (? extends T). The runtime check catches violations. This is a deliberate trade-off of safety for usability.\n\nFunction parameters are checked differently: parameter types are covariant in practice, return types are covariant.',
  ),
  InterviewQuestion(
    id: 'e_dart_2',
    topic: 'dart_fundamentals',
    subtopic: 'Async/Await',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'How do Zones work in Dart? What are they used for?',
    answer: 'Zones are execution contexts that intercept and modify the behavior of asynchronous operations. They can catch uncaught errors, override print, track async operations, and provide zone-local values. runZonedGuarded is used for global error handling in Flutter.',
    explanation: 'runZonedGuarded(() {\n  runApp(MyApp());\n}, (error, stack) {\n  // catches ALL uncaught async errors\n  crashReporter.report(error, stack);\n});\n\nZones intercept:\n- Timer creation (scheduleMicrotask, Timer)\n- Error handling (uncaught async errors)\n- print() calls\n- Zone-local values (like thread-local storage)\n\nFlutterError.onError handles framework errors; Zones handle Dart async errors.',
  ),
  InterviewQuestion(
    id: 'e_dart_3',
    topic: 'dart_fundamentals',
    subtopic: 'OOP',
    difficulty: Difficulty.expert,
    type: QuestionType.conceptual,
    question: 'What are extension types in Dart 3 and how do they differ from extension methods?',
    answer: 'Extension types create a zero-cost wrapper around an existing type with a different interface. Unlike extension methods (which add methods to a type), extension types create a completely new type that compiles away to the underlying type — no runtime overhead, no allocation.',
    explanation: 'extension type UserId(String id) {\n  bool get isValid => id.length > 0;\n}\nUserId uid = UserId("abc");\n// uid is a String at runtime, but the compiler treats it as UserId\n// Can\'t accidentally pass a raw String where UserId is expected\n\nUse cases: typed wrappers for IDs, validated types, FFI interop types, restricting APIs of existing types.',
  ),
];
